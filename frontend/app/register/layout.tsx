import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Create Account', description: 'Join Premium Store — create your free account to shop premium products, track orders, and get exclusive offers.' };
export default function RegisterLayout({ children }: { children: React.ReactNode }) { return children; }
