import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Track Package', description: 'Track your Premium Store order in real-time. Enter your tracking code to view delivery status via Steadfast Courier.' };
export default function TrackLayout({ children }: { children: React.ReactNode }) { return children; }
